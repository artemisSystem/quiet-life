{
	"private": true,
	"scripts": {
		"build": "spago build",
		"docs": "spago docs",
		"run_datagen": "spago run",
		"deploy": "npm run run_datagen && ./deploy.sh",
		"repl": "spago repl",
		"format": "purs-tidy format-in-place src"
	},
	"dependencies": {
		"kdljs": "^0.1.5",
		"sharp": "^0.31.3"
	},
	"devDependencies": {
		"purescript": "^0.15.7",
		"purescript-psa": "^0.8.2",
		"purs-tidy": "^0.9.2",
		"spago": "^0.20.9"
	}
}
